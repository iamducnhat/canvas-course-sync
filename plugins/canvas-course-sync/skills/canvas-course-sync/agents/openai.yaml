interface:
  display_name: "Canvas Course Sync (macOS)"
  short_description: "Sync assignments, due dates, and files"
  default_prompt: "Use $canvas-course-sync to ask for my Canvas domain URL and API token/key if missing, save the token securely, sync my courses, and summarize new assignments, announcements, and files."
policy:
  allow_implicit_invocation: true
